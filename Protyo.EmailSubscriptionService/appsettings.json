{
  "Logging": {
    "EventLog": {
      "LogLevel": {
        "Default": "Information"
      }
    }
  },

  "EmailSettings": {
     "SmtpServer": "${smtp_server}",
     "SmtpPort": ${smtpPort},
     "Username": "${smtp_username}",
     "Password": "${smtp_password }"

  },

  "GoogleAppSettings": {
    "SpreadsheetId": "${google_spreadsheet_id}",
    "SheetName": "Protyo Subscription Form (Responses)",
    "Values": "A:K"
  }
}
