{
  "Logging": {
    "EventLog": {
      "LogLevel": {
        "Default": "Information"
      }
    }
  },

  "DynamoSettings": {
    "AccessKeyId": "${access_key_id}",
    "SecretAccessKey": "${secret_access_key}"
  },
  
  "GrantAPIConfiguration": {
    "BaseUri": "${base_uri}",
  }
}
